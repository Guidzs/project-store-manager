const PRODUCT_NOT_FOUND = 'Product not found';
const NAME_INVALID = '"name" is required';
const MUST_LENGTH = '"name" length must be at least 5 characters long';

module.exports = {
  PRODUCT_NOT_FOUND,
  NAME_INVALID,
  MUST_LENGTH,
};
